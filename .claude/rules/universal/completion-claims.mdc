---
description: "完成声明和交付证据规则：禁止无证据声称完成或可交付"
globs: ["**/*"]
alwaysApply: true
---

---

description: "完成声明和交付证据规则：禁止无证据声称完成、可交付或无风险"
globs: ["**/*"]
alwaysApply: true
-----------------

# 完成声明规则

本规则适用于所有 Claude Code 编码、审查、重构、测试、文档和交付评估任务。

目标：确保 Claude Code 的完成声明始终基于证据，而不是基于产物存在、主观判断或未验证假设。

## 核心原则

* 产物存在不等于完成。
* 代码已修改不等于问题已解决。
* 验证通过不等于没有交付风险。
* 分数高不等于可交付。
* 只要硬门禁失败，就不能声明 Delivery Ready。
* 未验证的内容必须标记为未验证，不得包装成已完成项。

## 状态分级

* `L0 Unscoped`：目标、范围、验收标准未定义。
* `L1 Analyzed`：已分析目标、影响范围、关键假设和验证方式，但尚未实现。
* `L2 Implemented`：代码、文档或配置已实现，但尚未完成有效验证。
* `L3 Dry-run Verified`：基础验证、干跑、静态检查、局部测试或等价检查通过。
* `L4 Apply Verified`：在真实环境、集成环境或可接受的等价环境中验证通过。
* `L5 Delivery Ready`：证据齐全、风险闭环、硬门禁通过、无待确认交付阻塞项。
* `BLOCKED`：存在阻塞门禁，不能升级到更高状态。

## 状态升级条件

* 没有明确目标和验收标准时，最高只能到 `L0 Unscoped`。
* 完成分析但未修改产物时，最高只能到 `L1 Analyzed`。
* 已完成实现但没有运行验证时，最高只能到 `L2 Implemented`。
* 只完成本地检查、静态检查、单元测试、dry-run 或局部验证时，最高只能到 `L3 Dry-run Verified`。
* 已在真实环境、集成环境或等价环境中验证核心路径时，最高可到 `L4 Apply Verified`。
* 只有证据、风险、验收标准、人工确认事项和硬门禁全部闭环时，才能到 `L5 Delivery Ready`。

## 硬门禁

以下情况会阻止完成声明或阻止升级到更高状态：

* 没有验证报告：最高只能到 `L2 Implemented`。
* 存在与本次任务相关、由本次修改引入或影响验收结果的 error：不能声明完成。
* 存在新增 warning、与本次任务相关的 warning、或影响交付判断的 warning：不能声明 Delivery Ready。
* 缺少风险记录：不能说风险已闭环。
* 存在越界修改：必须进入人工 Review，不能声明 Delivery Ready。
* 缺少复现路径、验收标准或验证方式：不能说 bug 已完整修复。
* 存在未确认的产品、架构、数据、安全或上线决策：不能声明 Delivery Ready。
* 无法运行关键验证且没有可接受的替代证据：不能声明 Delivery Ready。

## Error 与 Warning 处理

* 新增 error 必须视为阻塞项。
* 与本次修改直接相关的 error 必须视为阻塞项。
* 历史遗留 error 不一定阻塞当前任务，但必须说明其来源、影响范围和与本次修改的关系。
* 环境、权限、依赖、外部服务导致的验证失败必须标记为 `BLOCKED` 或剩余风险。
* 新增 warning 或相关 warning 必须处理、解释或记录为风险。
* 历史遗留 warning 可以不阻塞当前任务升级，但必须说明其与本次修改无关。

## Risk Register 要求

非平凡任务必须提供风险记录。风险记录可以是轻量形式，但必须明确。

推荐格式：

```markdown
风险记录：
- 风险：<具体风险>
  影响：<影响范围>
  状态：<已处理 / 已接受 / 需人工确认 / 阻塞>
  证据：<验证命令、检查结果或说明>
```

小型低风险任务可以使用简化格式：

```markdown
风险记录：
- 未发现新增风险；修改仅限 <范围>，不涉及 <运行时逻辑/数据迁移/权限/安全边界>。
```

## 回复要求

完成任务或阶段性汇报时，必须说明：

* 当前状态级别和依据。
* 实际运行过的验证命令、结果和关键输出摘要。
* 无法运行验证的原因。
* 剩余 error、warning、风险、假设和未验证项。
* 需要人工确认的事项。
* 升级到下一状态所需动作。

不得使用以下表述，除非证据满足 `L5 Delivery Ready`：

* “已经完全完成”
* “无风险”
* “可以上线”
* “可直接交付”
* “全部验证通过”
* “没有问题”

可以使用更精确的表述：

* “当前达到 `L2 Implemented`，尚未运行验证。”
* “当前达到 `L3 Dry-run Verified`，本地测试通过，但未在真实环境验证。”
* “当前达到 `L4 Apply Verified`，核心路径已验证，仍有非阻塞风险需要人工确认。”
* “当前存在阻塞项，状态为 `BLOCKED`。”

## 推荐输出

```markdown
当前状态：L3 Dry-run Verified

依据：
- <为什么是该级别>

证据：
- <验证命令 1>：<结果>
- <验证命令 2>：<结果>

阻塞门禁：
- <无 / 有，列出具体项>

剩余 error / warning：
- <无 / 有，列出具体项>

风险记录：
- <risk register 摘要>

未验证项：
- <未覆盖路径、未运行环境、缺失依赖等>

需要人工确认：
- <无 / 有，列出具体事项>

下一步：
- <升级到 L4/L5 所需动作>
```
