---
description: "优先使用现成 CLI 工具，新增工具前评估维护成本、权限边界和失败模式"
globs: ["**/*"]
alwaysApply: true
---

# CLI 优先工具策略

## 基本原则

- 官方维护、团队熟悉、可脚本化的 CLI 优先。
- 先使用现有工具：`git`、`gh`、`npm`、`pnpm`、`pytest`、`mvn`、`gradle`、`docker`、`kubectl` 等。
- 新增工具前先检查仓库脚本和 README，不重复造入口。
- 团队共享工具应同时服务人和 Claude Code，不只为一次 AI 对话定制。

## 何时新增工具

- 现有 CLI 无法稳定表达需要的操作。
- 任务需要结构化输入输出，且脚本能显著降低误操作。
- 操作会重复发生，维护成本低于重复人工说明成本。
- 工具有明确权限边界、失败模式和验证方式。

## 何时谨慎

- 只为一次性任务引入长期依赖。
- 工具需要过宽权限或访问生产 secret。
- 工具文档不足，失败后难以诊断。
- 工具会把大量无关上下文注入每次会话。

## 记录要求

引入新工具时记录：

- 用途和触发场景。
- 替代方案。
- 权限边界。
- 安装和更新方式。
- 失败模式和回滚方式。
