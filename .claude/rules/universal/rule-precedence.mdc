---
description: "规则冲突优先级：定义安全、用户约束、项目约定、规则、技能和模板之间的裁决顺序"
globs: ["**/*"]
alwaysApply: true
---

# 规则冲突优先级

当 rules、skills、workflows、templates、项目文档或用户要求出现冲突时，按本规则裁决。

## 优先级

1. 安全、隐私、密钥和数据保护。
2. 用户明确约束。
3. 当前仓库已有架构、代码模式、配置和测试约定。
4. `rules/universal/*`。
5. `rules/frontend/*` 和 `rules/backend/*`。
6. 明确触发的 skills / workflows。
7. templates / examples / 示例说明。

## 裁决规则

- 高优先级规则覆盖低优先级建议。
- skill 或 workflow 不能覆盖 rule 中的强制约束。
- template 只提供文档结构，不定义新的工程规范。
- 用户要求若会泄露密钥、破坏数据、绕过安全或违反生产保护规则，必须先说明风险并拒绝或请求确认。
- 项目已有模式优先于通用偏好；只有现有模式明显有风险时，才提出替代方案。

## 冲突处理输出

发现冲突时，最终回复或报告应包含：

- 冲突项。
- 适用的更高优先级依据。
- 采用的裁决。
- 残余风险或需要人工确认的点。
