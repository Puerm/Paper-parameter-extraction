---
description: "密钥、生产配置和高风险生产操作保护规则"
globs: ["**/*"]
alwaysApply: true
---

# 密钥与生产保护

## 密钥边界

- 真实 `.env`、API Key、Token、私钥、数据库密码、生产账号和证书不进入仓库。
- 仓库只能提交 `.env.example`、`env.schema` 或配置说明。
- 前端不得保存后端密钥、模型 API Key、支付密钥、邮件密钥或对象存储密钥。
- 日志、截图、错误输出和最终报告中不得泄露 secret 原文。

## 生产配置

- 生产配置应保留在部署端，由部署流程读取。
- 新增环境变量必须同步更新 example/schema，并说明默认值、必填性和敏感级别。
- 配置变更必须说明影响面、验证方式和回滚方式。
- 不确定配置是否生产专用时，先停下确认，不要直接修改。

## 高风险操作

执行或建议以下操作前必须先报告风险并等待确认：

- 修改生产配置或密钥。
- 执行数据库迁移、回填、删除或不可逆数据操作。
- 重启、停止、reload 生产服务。
- 删除文件、清理目录、重置历史或强制覆盖远程状态。
- 修改公开 API、权限模型、支付链路、鉴权链路或审计链路。

## 完成声明

- 涉及配置、密钥、部署或数据变更时，最终报告必须包含验证证据和回滚方式。
- 无法验证生产影响时，只能声明“代码侧完成”或“待部署验证”，不能声明生产可交付。
