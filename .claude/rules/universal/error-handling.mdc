---
description: "团队通用错误处理规范"
globs: ["**/*"]
alwaysApply: true
---

---

description: "团队通用错误处理规范：显式处理、稳定响应、安全暴露、可追踪诊断"
globs: ["**/*"]
alwaysApply: true
-----------------

# 错误处理规范

本规则适用于所有前端、后端、脚本、测试、集成和自动化任务。

目标：确保错误被显式处理、可诊断、可恢复，并且不会向用户或外部调用方暴露敏感内部信息。

## 通用原则

* 错误必须被显式处理、向上传递，或转换为稳定响应。
* 不得静默吞掉错误。
* 用户可见错误应使用可理解文案。
* 内部日志应保留足够排查信息，但不得泄露敏感数据。
* 不在错误消息、HTTP 响应、日志、测试快照或异常包装中暴露密码、token、密钥、SQL、堆栈、内部路径、私有配置或第三方凭证。
* 异步流程必须考虑失败、超时、取消、重试、幂等性和并发竞态。
* 错误处理逻辑不得掩盖真实失败，不得把失败伪装成成功。

## 错误分类

实现错误处理时，应优先区分以下类型：

* `ValidationError`：输入格式错误、字段缺失、字段非法。
* `AuthenticationError`：未登录、凭证无效、会话过期。
* `AuthorizationError`：已认证但无权限执行操作。
* `NotFoundError`：目标资源不存在。
* `ConflictError`：唯一键冲突、版本冲突、并发冲突、状态冲突。
* `RateLimitError`：触发限流或配额限制。
* `TimeoutError`：请求、任务、外部调用或锁等待超时。
* `CancellationError`：用户取消、请求中止、任务被主动终止。
* `ExternalDependencyError`：第三方 API、消息队列、缓存、存储或下游服务异常。
* `BusinessRuleError`：违反业务规则，但不是系统异常。
* `InternalError`：未知异常、未预期异常或系统内部错误。

不得把所有错误都归类为未知异常。

## 前端规则

* UI 层必须区分 `loading`、`empty`、`error`、`success` 等状态。
* 表单校验错误应靠近对应字段展示。
* 网络错误、认证错误、权限错误、业务错误和未知错误应分开处理。
* React 项目应在合适层级使用 Error Boundary 捕获渲染错误。
* 异步请求应处理组件卸载、请求取消、重复提交、竞态响应和过期响应。
* 可恢复错误应提供重试、返回、重新登录或修改输入等明确操作。
* 禁止在组件中吞掉错误后只 `console.log`。
* 禁止把所有错误都展示为同一句无法行动的通用文案。
* 禁止向用户展示后端原始异常、堆栈、SQL、内部路径或第三方错误原文。

## 后端规则

* Controller / Handler 层负责将错误转换为统一 HTTP 状态码和响应体。
* Service 层不得返回模糊的 `null`、`undefined`、空字符串或魔法值表示失败；应使用明确错误类型、异常类型或 Result 模式。
* Domain 层应表达业务规则错误，而不是依赖数据库异常或通用异常间接表达业务失败。
* Unknown error 必须记录内部日志，并返回稳定的通用错误响应。
* 不得将未知异常、堆栈、SQL、内部类名、文件路径或依赖错误原文直接暴露给客户端。
* 禁止所有异常统一返回 HTTP 200。
* 禁止所有异常统一返回 HTTP 500。
* 禁止在底层捕获异常后返回伪成功结果。

## HTTP 响应原则

HTTP 状态码应表达错误类别：

* `400`：输入格式或参数错误。
* `401`：未认证或认证失效。
* `403`：无权限。
* `404`：资源不存在。
* `409`：状态冲突、并发冲突或唯一性冲突。
* `422`：语义正确但业务规则不满足。
* `429`：触发限流。
* `500`：未知内部错误。
* `502` / `503` / `504`：下游服务、依赖、网关或超时问题。

统一错误响应建议包含：

```json
{
  "code": "STABLE_ERROR_CODE",
  "message": "用户可理解的错误说明",
  "requestId": "request-id",
  "details": {}
}
```

要求：

* `code` 必须稳定，供前端、客户端或调用方识别。
* `message` 面向用户或调用方，不包含敏感内部细节。
* `requestId` 用于排查和跨服务追踪。
* `details` 只能包含安全、必要、可公开的字段。
* 不得返回 stack trace、SQL、内部路径、密钥、token、私有配置或第三方错误原文。

## 日志与可观测性

* 日志应包含 request id、trace id、任务 id、内部用户标识或其他可追踪字段。
* 日志应优先使用内部稳定标识或脱敏标识。
* 不记录密码、token、密钥、完整身份证号、完整银行卡号、完整手机号、完整邮箱、完整地址或其他敏感个人信息。
* 认证失败、权限失败、支付失败、外部服务失败、数据一致性异常和未知异常应具备可追踪日志。
* 高风险错误应配合指标、告警或审计日志。
* 日志级别应反映严重程度：可预期业务失败不应全部记录为系统错误。
* 不得为了隐藏错误而降低日志级别或删除诊断信息。

## 外部服务调用

* 所有外部服务调用必须设置超时。
* 重试必须设置次数上限。
* 重试必须考虑幂等性。
* 禁止对非幂等写操作进行盲目自动重试。
* 重试应使用 backoff 或 jitter，避免立即高频重试。
* 外部服务失败应明确区分可恢复失败、不可恢复失败和未知失败。
* 降级策略必须明确说明用户影响和数据影响。
* 超时、重试耗尽、降级和熔断应有日志或指标记录。

## 异步任务与后台流程

* 异步任务必须处理失败、超时、取消、重复执行和部分成功。
* 后台任务不得只在日志中失败而不更新任务状态。
* 长任务应记录状态：pending、running、succeeded、failed、cancelled 或 timed out。
* 可重试任务必须避免重复副作用。
* 批处理任务应记录成功数量、失败数量、失败原因和可重试项。
* 消息消费失败应明确 ack、nack、retry、dead-letter 策略。

## 禁止事项

* 禁止空 `catch`。
* 禁止捕获异常后只 `console.log` 或只打印堆栈。
* 禁止把所有异常统一返回 HTTP 200。
* 禁止把所有异常统一包装成未知错误。
* 禁止把未知异常直接暴露给客户端。
* 禁止无界重试。
* 禁止在失败时返回伪成功结果。
* 禁止用 `null`、`undefined`、空字符串、布尔值或魔法数字模糊表示复杂失败。
* 禁止因为测试难写而省略错误路径测试。

## 验证要求

涉及错误处理的修改，至少应验证以下内容：

* 正常路径仍然可用。
* 预期错误能返回稳定错误码和正确状态码。
* 未知错误不会暴露敏感内部信息。
* 日志中包含必要追踪字段。
* 日志和响应中不包含 token、密钥、SQL、堆栈或内部路径。
* 外部服务失败、超时或取消路径有明确行为。
* 前端能展示可理解错误，并提供必要恢复动作。
