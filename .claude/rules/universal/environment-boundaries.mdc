---
description: "本地、远程和 CI 环境边界：命令必须标注目标环境，避免误操作和重复启动服务"
globs: ["**/*"]
alwaysApply: true
---

# 环境边界

## 命令分区

- `[LOCAL]`：运行在开发机，用于编辑、测试、构建、本地调试和查看 diff。
- `[REMOTE-READONLY]`：运行在服务器，只允许查看状态、日志、磁盘、端口、进程和配置完整性。
- `[REMOTE-MUTATING]`：会改变服务器状态，例如 restart、reload、migrate、写配置、删文件，必须由用户明确授权。
- `[CI]`：运行在流水线，以 CI 配置和日志为准。

## 本地开发规则

- 本地命令先检查仓库脚本：`package.json`、`Makefile`、`scripts/`、`pyproject.toml`、`pom.xml`、`gradle`。
- Windows 开发机优先给 PowerShell 兼容命令。
- 不假设 Linux 路径、systemd、nginx 或生产目录存在于本地。
- 启动 dev server 前先检查端口和已有进程；已有服务运行时优先复用。
- 需要重启服务时先说明原因，并停止旧进程后再启动。

## 远程规则

- 默认只做只读诊断，不直接修改服务器状态。
- 查看日志、磁盘、进程、端口和服务状态时必须避免输出 secret。
- 不在服务器直接热修业务代码；业务代码只能通过 Git commit 和部署流程进入服务器。
- 生产服务重启、数据库迁移、配置写入和文件删除必须先给出风险、命令和回滚方式。

## 禁止事项

- 禁止不确认端口状态就连续启动多个前端、后端或 worker 实例。
- 禁止把本地命令当作服务器命令执行。
- 禁止把服务器路径、密钥路径或生产账号写进通用模板。
- 禁止对生产环境执行未标注目标环境和风险级别的命令。
