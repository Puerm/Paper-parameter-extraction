---
description: "CSS 与 Tailwind 使用规范：复用设计系统、响应式可靠、样式边界清楚"
globs: ["**/*.css", "**/*.scss", "**/*.tsx", "**/*.jsx"]
alwaysApply: false
---

# CSS 与 Tailwind 规范

## 通用原则

- 优先复用项目设计系统、组件库、主题变量和已有工具类。
- 样式表达布局和状态，不把业务逻辑藏进 class 拼接。
- 响应式布局覆盖移动端、桌面端和内容溢出场景。
- 文本、按钮、卡片和固定格式控件应有稳定尺寸或约束，避免布局跳动。

## Tailwind

- 常用组合抽成组件、variant 或工具函数，避免超长 class 重复出现。
- 条件 class 使用项目约定工具，例如 `cn`、`clsx` 或 `classnames`。
- 颜色、间距、字号优先使用设计 token，不随意写一次性值。
- 不为了局部效果破坏全局视觉节奏。

## CSS

- 全局样式只放 reset、主题变量和真正全局规则。
- 局部样式避免影响未知后代。
- 动画考虑 `prefers-reduced-motion`。

## 禁止事项

- 禁止靠 `!important` 掩盖层级问题。
- 禁止使用难以维护的大段内联 style。
- 禁止固定宽高导致文本溢出或移动端不可用。
