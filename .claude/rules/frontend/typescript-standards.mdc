---
description: "TypeScript 编码规范：类型清晰、少断言、导出边界明确"
globs: ["**/*.ts", "**/*.tsx"]
alwaysApply: false
---

# TypeScript 规范

## 类型安全

- 遵守项目当前 `strict` 配置；不要为通过编译降低类型检查。
- 公共函数、导出函数和公共组件 props 应有明确类型。
- 避免 `any`；兼容未知输入时使用 `unknown` 并收窄。
- 类型断言用于表达已验证事实，不用于绕过真实不确定性。
- 可选字段和 nullable 字段语义要清楚，不混用 `undefined` 与 `null`。

## 类型组织

- 只被单文件使用的类型放在本文件。
- 多处复用的类型放在项目约定的 `types/`、`models/` 或模块内类型文件。
- API 请求和响应类型应与接口客户端、schema 或 OpenAPI 保持同源。
- 不导出未被外部使用或语义不清的公共类型。

## 函数与泛型

- 泛型名称表达含义，例如 `TData`、`TError`、`TResult`。
- 参数超过 3 个时，优先考虑对象参数，但不为很小的局部函数强行抽象。
- 分支复杂时优先用类型守卫、schema 校验或 discriminated union 表达约束。

## 禁止事项

- 禁止用 `as any` 绕过类型系统。
- 禁止为通过编译删除真实错误处理。
- 禁止用宽泛类型掩盖接口契约变化。
