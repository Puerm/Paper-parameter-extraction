---
description: "React 组件开发规范：组件聚焦、Hook 正确、状态和交互明确"
globs: ["src/components/**/*.tsx", "src/features/**/*.tsx", "app/**/*.tsx"]
alwaysApply: false
---

# React 组件规范

## 组件结构

- 一个文件默认聚焦一个主要组件；小型私有子组件可同文件放置。
- 组件、props 类型和辅助函数按项目既有顺序组织。
- 组件导出方式遵守项目约定；没有约定时优先 named export。
- Props 类型命名为 `<ComponentName>Props`。

## Hooks

- Hook 只在组件或自定义 Hook 顶层调用。
- `useEffect` 依赖数组应完整；故意省略依赖时说明原因。
- 副作用涉及订阅、定时器、请求取消或外部资源时应清理。
- 自定义 Hook 使用 `use` 前缀，并返回稳定、可理解的数据结构。

## 状态与数据

- 优先使用局部 state；跨组件状态遵守项目既有状态管理方案。
- 不把可从 props、server state 或 URL 推导出的值重复存入 state。
- 服务端数据、缓存、乐观更新和错误处理遵守项目现有数据获取模式。

## UI 与交互

- 表单、按钮、弹窗和菜单应具备键盘可用性。
- 用户可感知流程应显式呈现错误、加载和空状态。
- 列表渲染使用稳定 key；可变列表避免使用 index 作为 key。
- 不在组件中吞掉错误；按项目约定展示、上报或向上抛出。
