---
description: "微服务与后台任务规范：契约清楚、调用有边界、任务可观测"
globs: ["**/*.{ts,js,java,kt,go,py}", "services/**/*", "workers/**/*", "jobs/**/*"]
alwaysApply: false
---

# 微服务与后台任务规范

## 服务边界

- 跨服务通信通过明确契约，避免共享内部模型或数据库表。
- 服务拥有者、数据所有权和故障责任应清楚。
- 跨服务字段、事件和错误码变化按兼容性变更处理。
- 服务间传递的 ID 应是稳定业务标识，除非项目已有内部主键约定。

## 调用可靠性

- 服务间调用设置超时。
- 可重试操作必须具备幂等性；不可重试操作说明原因。
- 同步调用链过长时，评估异步、队列、缓存或聚合层。
- 降级、重试和熔断策略遵守项目已有基础设施。

## 后台任务

- 后台任务应有所有者、触发条件、重试策略、超时和告警。
- 批处理任务记录处理范围、成功失败数量和可追踪 ID。
- 定时任务避免重复执行导致数据重复变更。

## 禁止事项

- 禁止没有所有者、日志和失败处理的后台任务进入生产。
- 禁止同步调用链无限拉长。
- 禁止跨服务直接读写对方数据库，除非项目已有明确架构约定。
