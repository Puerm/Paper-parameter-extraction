---
description: "输入规范化与内容安全：边界校验、存储策略、渲染安全"
globs: ["**/*.{ts,tsx,js,jsx,java,kt,go,py}"]
alwaysApply: false
---

# 输入规范化与内容安全

## 输入边界

- 外部输入在系统边界处校验类型、格式、长度、范围和枚举值。
- 字符串输入按业务需要处理前后空白、大小写、Unicode 规范化和空字符串。
- ID、金额、时间、分页、排序和过滤参数使用明确校验规则。
- 不让非法输入进入 service、domain、repository 或外部依赖调用层。

## 内容安全

- 用户生成内容进入数据库前明确安全策略：纯文本、转义、净化或白名单。
- 如果业务需要 HTML，使用可信 sanitizer 和标签白名单。
- 渲染用户内容时按上下文处理 HTML、URL、Markdown、富文本和文件名。
- 日志和错误响应中避免输出未净化的用户输入。

## 禁止事项

- 禁止把“后端已经校验”当作前端安全渲染的理由。
- 禁止把“前端已经转义”当作后端存储安全的唯一防线。
- 禁止将未校验输入拼接进 SQL、命令、路径、模板或 HTML。
