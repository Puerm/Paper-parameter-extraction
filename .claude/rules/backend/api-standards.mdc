---
description: "后端 API 设计与实现规范：资源化路由、契约稳定、输入校验和兼容性"
globs: ["src/**/*.{ts,js,java,kt,go,py}", "app/**/*.{ts,js,java,kt,go,py}", "routes/**/*.{ts,js,java,kt,go,py}", "controllers/**/*.{ts,js,java,kt,go,py}", "handlers/**/*.{ts,js,java,kt,go,py}", "api/**/*.{yaml,yml,json,md}", "openapi/**/*.{yaml,yml,json}", "docs/api/**/*.md"]
alwaysApply: false
---

# API 规范

本规则适用于后端 HTTP API、Controller、Router、Handler、DTO、Schema、OpenAPI 文档和 API 契约测试。

## 路由与方法

- 使用资源名词和 HTTP 方法表达动作。
- 路径使用稳定业务词，不暴露内部实现、数据库表名或框架细节。
- 路径层级保持简单；嵌套资源通常不超过 2 层。
- 过滤、排序、分页使用查询参数。
- 状态迁移和业务动作应有清楚资源语义，例如 `POST /orders/{orderId}/cancel`。
- `GET` 用于读取，不产生业务副作用。
- `POST` 用于创建资源、提交命令或执行非幂等操作。
- `PUT` 用于整体替换，通常应具备幂等性。
- `PATCH` 用于局部更新。
- `DELETE` 用于删除、撤销或移除资源关系。

## 请求与响应契约

- DTO、schema 或 OpenAPI 应显式描述请求体、响应体、字段类型、必填性、默认值和示例。
- 输入校验在进入业务逻辑前完成。
- 请求字段和响应字段遵守项目既有序列化风格。
- 时间、金额、数量、比例、精度、时长、距离、容量等字段说明格式、单位和时区语义。
- 响应体不直接依赖数据库实体序列化；通过 DTO、schema 或 mapper 控制外部契约。
- 不向客户端返回内部字段、调试字段、密钥、权限内部实现或审计内部信息。

## 分页、过滤与排序

- 可能增长的列表接口默认分页；规模受限时说明原因。
- 分页参数有默认值和最大值。
- 排序字段、过滤字段使用白名单。
- 不允许客户端传入任意 SQL、字段名、表达式或未校验排序条件。
- 响应中的分页信息遵守项目统一格式，例如 `items`、`total`、`nextCursor`、`hasMore`。

## 幂等性与重复请求

- 创建订单、支付、退款、回调处理、消息消费、状态迁移等操作考虑重复请求。
- 外部回调和 webhook 基于 provider event id、业务唯一键或幂等键去重。
- 可能被客户端重试的写操作，优先支持 `Idempotency-Key` 或项目统一幂等机制。
- 非幂等操作说明原因和风险。
- 不把“前端不会重复提交”作为后端正确性的前提。

## 认证、授权与兼容性

- API 使用项目统一认证机制。
- 需要权限控制的接口经过统一授权检查；资源级权限在后端校验。
- 高风险操作记录审计信息，例如支付、退款、权限变更、身份变更、数据导出、删除和配置变更。
- API 字段删除、改名、类型变化、单位变化、语义变化、状态码变化和权限语义变化，都按兼容性变更处理。
- 破坏性变更需要版本、迁移策略、兼容期、feature flag 或明确的内部 API 说明。

## OpenAPI 与契约测试

- 对外 API 或跨团队 API 应维护 OpenAPI、schema、契约测试或等价契约文档。
- 新增或修改 API 时，同步更新示例、错误码、状态码和验证规则。
- API 实现、DTO、OpenAPI 文档和测试保持一致。

## 验证

涉及 API 的修改，至少考虑验证：

- 正常请求返回预期状态码和响应体。
- 输入非法时返回稳定错误码。
- 未认证、无权限、资源不存在、状态冲突等错误路径可区分。
- 分页、过滤、排序参数受到校验。
- 响应不暴露内部堆栈、SQL、密钥、token、内部路径或第三方错误原文。
- 破坏性变更已有版本、迁移或兼容策略。

## 禁止事项

- 禁止使用 `GET` 执行删除、支付、退款、状态变更等有副作用操作。
- 禁止返回 HTTP 200 表示失败。
- 禁止绕过统一鉴权、授权和审计中间件。
- 禁止直接序列化数据库实体作为外部响应。
- 禁止无分页返回可能增长的大列表。
- 禁止把破坏性 API 变更伪装成普通重构。
