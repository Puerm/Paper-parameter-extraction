---
description: "限流与实时能力规则：高频动作有边界，广播内容可授权"
globs: ["**/*.{ts,js,java,kt,go,py}", "realtime/**/*", "websocket/**/*", "workers/**/*"]
alwaysApply: false
---

# 限流与实时规则

## 限流

- 高频用户操作应有限流策略，例如评论、点赞、送礼、验证码、登录和搜索。
- 限流 key 根据业务包含用户、资源、动作、IP 或设备维度。
- 限流窗口设置 TTL，避免状态永久堆积。
- 限流错误返回稳定错误码和可理解信息。

## 实时与广播

- 广播内容经过权限、归属和内容安全检查。
- 广播给多个用户前，确认不包含仅单个用户可见的敏感字段。
- WebSocket、SSE、长轮询或消息推送应明确连接、重连、鉴权、心跳和取消策略。
- 客户端可重放或重连时，事件 ID、游标或状态查询方式应清楚。

## 一致性

- 送礼、扣费、积分变更等实时动作要保证扣减、记录和广播的一致性。
- 广播失败不应破坏已提交的核心业务状态；需要补偿时记录策略。

## 禁止事项

- 禁止无限制高频写操作直接进入核心业务。
- 禁止向公共频道广播私有字段。
- 禁止用实时推送替代后端权限校验。
