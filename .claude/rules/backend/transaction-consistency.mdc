---
description: "事务与一致性规则：边界明确、并发安全、状态可追踪"
globs: ["**/*.{ts,js,java,kt,go,py}", "migrations/**/*", "db/**/*"]
alwaysApply: false
---

# 事务与一致性规则

## 事务边界

- 写操作涉及多个状态变更时，先明确事务边界。
- 在抛出异常前写入的安全状态，应确认事务提交或回滚路径。
- 事务中避免调用不可控外部服务；必须调用时说明超时、重试和补偿策略。
- 事务外副作用例如消息、邮件、广播，应考虑 outbox、事件表或补偿机制。

## 并发与幂等

- 金额、库存、积分、余额、失败次数、名额等状态更新考虑并发。
- 已有模块存在锁、版本号或唯一约束模式时，新代码优先复用同一模式。
- 支付、充值、结算、送礼、回调和消息消费等流程应有重复请求防护。
- 幂等键、业务唯一键和外部事件 ID 的语义要清楚。

## 删除与恢复

- 使用软删除时，唯一约束要明确是否排除已删除记录。
- “删除后重新创建”需要覆盖唯一性、权限、审计和恢复行为。
- 物理删除涉及用户数据、账务、审计或外部依赖时，先评估保留和合规要求。

## 链路追踪

- 定义 Service 方法不代表流程接通；关键业务链路要从入口追踪到最终状态变更。
- 修改状态机时检查所有入口、回调、定时任务和重试路径。
- 关键链路的验证应覆盖成功、重复、失败和部分失败场景。

## 禁止事项

- 禁止依赖“前端不会重复提交”保证一致性。
- 禁止在没有补偿策略的情况下把本地事务和外部副作用混在一起。
- 禁止只测成功路径就声明关键写流程完成。
