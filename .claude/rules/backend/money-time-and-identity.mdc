---
description: "金额、时间与身份规则：单位明确、时区一致、凭证语义清楚"
globs: ["**/*.{ts,js,java,kt,go,py}", "migrations/**/*", "db/**/*"]
alwaysApply: false
---

# 金额、时间与身份规则

## 金额与数量

- 资金、积分兑换金额、结算金额等高精度数值避免使用浮点数。
- 字段名或 schema 应体现单位、币种或精度，例如 `amountCents`、`priceMicros`。
- 金额变更记录来源、方向、关联业务单据和操作者。
- 金额相关逻辑考虑并发、幂等、审计和回滚。

## 时间

- 时间字段说明时区语义，优先使用项目统一格式。
- 过期、锁定、结算窗口、冷却时间和调度时间不混用本地时区与 UTC。
- 时间段字段体现单位，例如 `expiresInSeconds`、`timeoutMillis`。
- 测试中避免依赖真实当前时间；优先使用可注入时钟或固定时间。

## 身份与凭证

- 密码使用安全哈希算法和独立 salt，不明文存储，不使用可逆加密。
- Access token、refresh token、一次性 token 区分用途、有效期和吊销语义。
- 用户 ID、账号 ID、设备 ID、会话 ID 和外部 provider ID 不混用。
- 权限判断以服务端可信身份为准，不依赖客户端声明。

## 禁止事项

- 禁止用 `number` / `float` 表示资金。
- 禁止不同 token 类型共用一套校验语义。
- 禁止把客户端传入的身份字段当作已认证身份。
