---
description: "数据库规范：schema 稳定、查询安全、迁移可审查"
globs: ["**/*.{sql,prisma,ts,js,java,kt,go,py}", "migrations/**/*", "db/**/*"]
alwaysApply: false
---

# 数据库规范

## Schema 设计

- 表名、列名、索引和约束命名遵守项目既有约定。
- 字段类型表达真实业务语义，不用字符串承载结构化或可枚举状态。
- 枚举值应有代码常量、schema 或文档来源。
- 金额、时间、身份、软删除和审计字段遵守对应领域规则。

## 查询

- 用户输入必须参数化，不拼接 SQL。
- 分页查询应有稳定排序。
- 可能增长的查询应有边界、索引或明确的规模说明。
- 避免在事务中调用不可控外部服务。

## 迁移

- 迁移脚本应可审查、可回滚或有补偿方案。
- 删除字段、改类型、改语义属于破坏性变更，先评估兼容性。
- 数据修复脚本记录执行范围、执行时间、验证方式和回滚考虑。

## 禁止事项

- 禁止生产环境直接手工改表而不沉淀迁移或记录。
- 禁止把敏感数据明文落库。
- 禁止把数据库异常当作常规输入校验。
